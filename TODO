NOW:
----
-> Implement interface operations
    - load: Load a ROM file to the chi8 ram
    - init: Initialize all registers
    - get_keys: Get all pressed keys
    - update_timers: Update delay and sound timers
    - exec_cicle: Execute the next CPU cicle
    - draw_display: Draw the display buffer in the window

FAR:
----
-> Implement the main loop
  - Gather all interface operations together
